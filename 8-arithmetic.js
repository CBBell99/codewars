function arithmetic(a, b, operator) {
  //your code here!
  if (operator == "add") {
    return a + b;
  }
  if (operator == "subtract") {
    return a - b;
  }
  if (operator == "multiply") {
    return a * b;
  } else {
    return a / b;
  }
}
